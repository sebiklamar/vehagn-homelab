apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: tls-passthrough
  namespace: gateway
spec:
  gatewayClassName: cilium
  infrastructure:
    annotations:
      io.cilium/lb-ipam-ips: 10.7.4.231
  listeners:
    - protocol: TLS
      port: 443
      name: proxmox
      hostname: "proxmox.stonegarden.iseja.net"
      tls:
        mode: Passthrough
      allowedRoutes:
        namespaces:
          from: All
    - protocol: TLS
      port: 443
      name: truenas
      hostname: "truenas.stonegarden.iseja.net"
      tls:
        mode: Passthrough
      allowedRoutes:
        namespaces:
          from: All
